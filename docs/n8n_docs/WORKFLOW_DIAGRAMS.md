# AI-EWG Workflow Diagrams

Visual guides for understanding and implementing n8n workflows with AI-EWG.

---

## 📊 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         VIDEO SOURCES                            │
│  Local Files │ Network Shares │ Cloud Storage │ Upload Folder   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DISCOVERY LAYER                             │
│  - File scanning                                                 │
│  - Hash computation (deduplication)                              │
│  - Metadata extraction                                           │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    PROCESSING PIPELINE                           │
│                                                                  │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐    │
│  │  Prep    │ → │Transcribe│ → │ Enrich   │ → │ Render   │    │
│  │ Validate │   │ Whisper  │   │  Ollama  │   │   HTML   │    │
│  └──────────┘   └──────────┘   └──────────┘   └──────────┘    │
│       1s            5-8 min         2-5 min         10s         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    CLIP GENERATION                               │
│                                                                  │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐    │
│  │  Topic   │ → │  Score   │ → │  Select  │ → │  Render  │    │
│  │Segment   │   │Sentences │   │   Best   │   │  Videos  │    │
│  └──────────┘   └──────────┘   └──────────┘   └──────────┘    │
│       5s            10s            2s            2-5 min        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                        OUTPUTS                                   │
│  HTML Pages │ Video Clips │ Transcripts │ Metadata │ Subtitles  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Processing Flow (Detailed)

```
START
  │
  ▼
┌─────────────────────┐
│  Discover Videos    │  POST /episodes/discover
│  Duration: 1-5s     │  Returns: episode_id
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Submit Async Job   │  POST /async/episodes/{id}/process
│  Duration: <1s      │  Returns: job_id (immediately)
└──────────┬──────────┘
           │
           ├──────────────────────────────────────┐
           │                                      │
           ▼                                      ▼
┌─────────────────────┐              ┌─────────────────────┐
│  HTTP Response      │              │  Background Thread  │
│  Returns job_id     │              │  Starts Processing  │
└─────────────────────┘              └──────────┬──────────┘
                                                │
                                                ▼
                                     ┌─────────────────────┐
                                     │  Stage 1: Prep      │
                                     │  Validate source    │
                                     │  Progress: 5%       │
                                     └──────────┬──────────┘
                                                │
                                                ▼
                                     ┌─────────────────────┐
                                     │  Stage 2: Whisper   │
                                     │  Speech-to-text     │
                                     │  Progress: 10-40%   │
                                     └──────────┬──────────┘
                                                │
                                                ▼
                                     ┌─────────────────────┐
                                     │  Stage 3: Ollama    │
                                     │  AI enrichment      │
                                     │  Progress: 40-80%   │
                                     └──────────┬──────────┘
                                                │
                                                ▼
                                     ┌─────────────────────┐
                                     │  Stage 4: Render    │
                                     │  Generate HTML      │
                                     │  Progress: 80-100%  │
                                     └──────────┬──────────┘
                                                │
                                                ▼
                                     ┌─────────────────────┐
                                     │  Job Complete       │
                                     │  Trigger Webhook    │
                                     └─────────────────────┘
```

---

## 🎬 Clip Generation Flow

```
Episode (transcribed)
  │
  ▼
┌─────────────────────────────────────────┐
│  POST /episodes/{id}/discover_clips     │
│                                         │
│  Input Parameters:                      │
│  - max_clips: 5                         │
│  - min_duration_ms: 20000               │
│  - max_duration_ms: 120000              │
└──────────────────┬──────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│  Step 1: Topic Segmentation             │
│  - Generate sentence embeddings         │
│  - Detect topic boundaries              │
│  - Create segments (20s - 2min)         │
└──────────────────┬──────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│  Step 2: Sentence Scoring               │
│  - Coherence (30%)                      │
│  - Information density (25%)            │
│  - Engagement (20%)                     │
│  - Quotability (15%)                    │
│  - Duration (10%)                       │
└──────────────────┬──────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│  Step 3: Clip Selection                 │
│  - Sort by score                        │
│  - Filter duplicates                    │
│  - Ensure diversity                     │
│  - Select top N clips                   │
└──────────────────┬──────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│  Returns: Clip Specifications           │
│  - clip_id                              │
│  - start_ms, end_ms                     │
│  - title, caption                       │
│  - hashtags                             │
│  - score                                │
└──────────────────┬──────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│  POST /async/episodes/{id}/render_clips │
│                                         │
│  Input:                                 │
│  - clip_ids: ["clip_001", ...]          │
│  - variants: ["clean", "subtitled"]     │
│  - aspect_ratios: ["16:9", "9:16"]      │
└──────────────────┬──────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│  Background Rendering                   │
│  For each clip × variant × ratio:       │
│  1. Extract video segment               │
│  2. Apply variant (subtitles/branding)  │
│  3. Convert aspect ratio                │
│  4. Export MP4 file                     │
└──────────────────┬──────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│  Output Files:                          │
│  - clip_001_clean_16x9.mp4              │
│  - clip_001_clean_9x16.mp4              │
│  - clip_001_subtitled_16x9.mp4          │
│  - clip_001_subtitled_9x16.mp4          │
└─────────────────────────────────────────┘
```

---

## 🤖 n8n Workflow Pattern 1: Simple Polling

```
┌─────────────────────┐
│  Schedule Trigger   │  Daily at 2 AM
│  (Cron: 0 2 * * *)  │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  HTTP Request       │  POST /episodes/discover
│  Discover Episodes  │  → Returns: episode_ids[]
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Split in Batches   │  Process 5 at a time
│  Batch Size: 5      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  HTTP Request       │  POST /async/episodes/{id}/process
│  Submit Job         │  → Returns: job_id
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Set Variable       │  Store job_id
│  job_id = {{$json}} │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Wait               │  30 seconds
│  Duration: 30s      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  HTTP Request       │  GET /async/jobs/{{job_id}}
│  Check Status       │  → Returns: status, progress
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  IF Node            │  status == "completed"?
│  Check Complete     │
└──────┬───────┬──────┘
       │       │
    NO │       │ YES
       │       │
       ▼       ▼
    ┌─────┐  ┌─────────────────────┐
    │Loop │  │  HTTP Request       │  GET /episodes/{id}
    │Back │  │  Get Episode Data   │  → Returns: full episode
    └─────┘  └──────────┬──────────┘
                        │
                        ▼
             ┌─────────────────────┐
             │  Slack/Email        │  Send notification
             │  Notification       │  "Episode processed!"
             └─────────────────────┘
```

---

## 🔔 n8n Workflow Pattern 2: Webhook Notification

```
┌─────────────────────┐
│  Webhook Node       │  Create webhook endpoint
│  Listen Mode        │  URL: https://n8n.com/webhook/abc123
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Set Variable       │  Store webhook URL
│  webhook_url = ...  │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  HTTP Request       │  POST /episodes/discover
│  Discover Episodes  │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  HTTP Request       │  POST /async/episodes/{id}/process
│  Submit Job         │  Body: { webhook_url: "..." }
│  + Webhook URL      │  → Returns: job_id
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Wait for Webhook   │  Workflow pauses here
│  Timeout: 1 hour    │
└──────────┬──────────┘
           │
           │ (Webhook fires when job completes)
           │
           ▼
┌─────────────────────┐
│  Webhook Received   │  Payload: { job_id, status, result }
│  Parse Payload      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  IF Node            │  status == "completed"?
│  Check Success      │
└──────┬───────┬──────┘
       │       │
    NO │       │ YES
       │       │
       ▼       ▼
    ┌─────┐  ┌─────────────────────┐
    │Error│  │  HTTP Request       │  GET /episodes/{id}
    │Alert│  │  Get Episode Data   │
    └─────┘  └──────────┬──────────┘
                        │
                        ▼
             ┌─────────────────────┐
             │  Process Results    │  Extract metadata
             │  Extract Data       │
             └──────────┬──────────┘
                        │
                        ▼
             ┌─────────────────────┐
             │  Slack Notification │  "✅ Episode ready!"
             │  + Summary          │  Include title, duration
             └─────────────────────┘
```

---

## 📦 n8n Workflow Pattern 3: Batch Processing

```
┌─────────────────────┐
│  Schedule Trigger   │  Daily at 2 AM
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  HTTP Request       │  POST /episodes/discover
│  Discover All       │  → Returns: episodes[]
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Filter             │  Only "discovered" stage
│  New Episodes Only  │  stage == "discovered"
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Split in Batches   │  Process 3 at a time
│  Batch Size: 3      │  (Parallel processing)
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Loop Over Batch    │  For each episode
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  HTTP Request       │  POST /async/episodes/{id}/process
│  Submit Job         │  → job_id
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Store Job IDs      │  Collect all job_ids
│  Array Push         │  jobs[] = job_id
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Wait               │  5 minutes
│  Duration: 5min     │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Loop Over Jobs     │  Check each job
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  HTTP Request       │  GET /async/jobs/{job_id}
│  Check Status       │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  IF Complete        │  All jobs done?
└──────┬───────┬──────┘
       │       │
    NO │       │ YES
       │       │
       ▼       ▼
    ┌─────┐  ┌─────────────────────┐
    │Wait │  │  Aggregate Results  │  Collect all episodes
    │Loop │  │  Summary Stats      │  Count: success/failed
    └─────┘  └──────────┬──────────┘
                        │
                        ▼
             ┌─────────────────────┐
             │  Email Report       │  Daily summary
             │  "3 videos processed"│  Include links
             └─────────────────────┘
```

---

## 🎥 n8n Workflow Pattern 4: Full Content Pipeline

```
┌─────────────────────┐
│  Schedule Trigger   │  Daily at 9 AM
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Discover Episodes  │  Find new videos
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Process Episode    │  Transcribe + Enrich
│  (Async + Poll)     │  Wait for completion
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Discover Clips     │  Find best segments
│  max_clips: 5       │  20-60 seconds each
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Render Clips       │  Generate 9:16 videos
│  (Async + Poll)     │  Subtitled variant
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Get Clip Files     │  Retrieve file paths
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Loop Over Clips    │  For each clip
└──────────┬──────────┘
           │
           ├──────────────┬──────────────┬──────────────┐
           │              │              │              │
           ▼              ▼              ▼              ▼
    ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
    │ TikTok   │   │Instagram │   │ YouTube  │   │ Twitter  │
    │ Upload   │   │  Reels   │   │ Shorts   │   │  Post    │
    └──────────┘   └──────────┘   └──────────┘   └──────────┘
           │              │              │              │
           └──────────────┴──────────────┴──────────────┘
                          │
                          ▼
               ┌─────────────────────┐
               │  Slack Notification │  "✅ 5 clips posted!"
               │  + Analytics Links  │
               └─────────────────────┘
```

---

## 🔍 Monitoring Dashboard Flow

```
User Opens Streamlit Dashboard
           │
           ▼
┌─────────────────────────────────────────┐
│  Navigation Sidebar                     │
│  - Process Videos (Manual)              │
│  - Job Monitor (View Status)            │
│  - Episode Browser                      │
│  - Clip Gallery                         │
└──────────────────┬──────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│  Job Monitor Page (Selected)            │
│                                         │
│  Tab 1: Active Jobs                     │
│  ┌─────────────────────────────────┐   │
│  │ Job: process_episode            │   │
│  │ Progress: ████████░░ 45%        │   │
│  │ Stage: transcribing             │   │
│  │ ETA: 3m 20s                     │   │
│  └─────────────────────────────────┘   │
│                                         │
│  Tab 2: Completed Jobs                  │
│  ┌─────────────────────────────────┐   │
│  │ ✅ Episode processed             │   │
│  │ Duration: 342s                  │   │
│  │ [View Episode] [View Clips]     │   │
│  └─────────────────────────────────┘   │
│                                         │
│  Tab 3: Episodes                        │
│  ┌─────────────────────────────────┐   │
│  │ 📹 Episode 01 - rendered         │   │
│  │ [View HTML] [View Transcript]   │   │
│  └─────────────────────────────────┘   │
│                                         │
│  Tab 4: Clips                           │
│  ┌─────────────────────────────────┐   │
│  │ 🎬 Clip 1: Key Insight           │   │
│  │ Duration: 30s | Score: 0.87     │   │
│  │ Files: 4 variants               │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

---

## 🎯 Decision Tree: Which Workflow Pattern?

```
START: Need to process videos?
  │
  ├─ Single video, manual → Use Streamlit Dashboard
  │                         (Process Videos page)
  │
  ├─ Single video, automated → Pattern 1: Simple Polling
  │                            (n8n with HTTP nodes)
  │
  ├─ Multiple videos, scheduled → Pattern 3: Batch Processing
  │                                (n8n with cron trigger)
  │
  ├─ Need immediate notification → Pattern 2: Webhook
  │                                 (n8n with webhook node)
  │
  └─ Full content pipeline → Pattern 4: Full Pipeline
                             (Process + Clips + Publish)
```

---

## 📊 Performance Considerations

```
Concurrent Processing Capacity
┌─────────────────────────────────────────┐
│  Single Worker (Default)                │
│  ┌────────┐                             │
│  │ Job 1  │ ████████████ (12 min)       │
│  └────────┘                             │
│  ┌────────┐                             │
│  │ Job 2  │             ████████████    │
│  └────────┘                             │
│  Total: 24 minutes for 2 videos         │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  Two Workers (Parallel)                 │
│  ┌────────┐                             │
│  │ Job 1  │ ████████████ (12 min)       │
│  └────────┘                             │
│  ┌────────┐                             │
│  │ Job 2  │ ████████████ (12 min)       │
│  └────────┘                             │
│  Total: 12 minutes for 2 videos         │
│  Requires: 16GB+ RAM                    │
└─────────────────────────────────────────┘
```

---

## 🚀 Quick Start Flow

```
1. Start API Server
   python src/cli.py --config config/pipeline.yaml api --port 8000
   
2. Verify Health
   curl http://localhost:8000/health
   
3. Test Discovery
   curl -X POST http://localhost:8000/episodes/discover
   
4. Submit Test Job
   curl -X POST http://localhost:8000/async/episodes/{id}/process \
     -H "Content-Type: application/json" \
     -d '{"target_stage": "rendered"}'
   
5. Monitor Progress
   curl http://localhost:8000/async/jobs/{job_id}
   
6. View Results
   Open: data/outputs/{show}/{episode_id}/index.html
```

---

**For more details, see:**
- `N8N_DEVELOPER_GUIDE.md` - Complete technical documentation
- `API_QUICK_REFERENCE.md` - API endpoint reference
- `HYBRID_WORKFLOW_IMPLEMENTATION.md` - Architecture details
