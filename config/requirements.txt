# Video Processing Dependencies
# Optimized for: AMD Threadripper PRO 5995WX (64 cores) + RTX 4080 + 128GB RAM

# CUDA support for GPU acceleration (RTX 4080)
torch==2.1.0+cu121
torchaudio==2.1.0+cu121

# Faster-Whisper: MUCH faster than openai-whisper, GPU-optimized
# Uses CTranslate2 for up to 4x speed improvement
faster-whisper>=0.10.0

# WhisperX (optional, for advanced features like speaker diarization)
# whisperx>=3.1.1

# Additional utilities
pyyaml>=6.0
psutil>=5.9.0

# For better performance
numpy>=1.24.0

# ========================================
# AI Enrichment System Dependencies
# ========================================

# Speaker Diarization
pyannote.audio>=3.1.0
huggingface-hub>=0.20.0

# Entity Extraction - spaCy
spacy>=3.7.0
en-core-web-lg @ https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.7.0/en_core_web_lg-3.7.0-py3-none-any.whl

# Entity Extraction - LLM (Ollama local)
requests>=2.31.0

# Disambiguation & Matching
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.23.0

# Wikipedia/Wikidata API access
wikipediaapi>=0.6.0

